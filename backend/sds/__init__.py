"""Surface Découplée Système (SDS) — package marker.

HOS-000 foundation deliverable. The SDS is the FastAPI-side service
surface that consumes whatever runtime the RAL selects. The first
SDS route (``/api/hermes-os/health`` and siblings) is delivered by
HOS-003; the SDS remains empty until then.
"""
__all__: list[str] = []
